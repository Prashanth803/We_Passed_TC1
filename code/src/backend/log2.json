{
  "name": "getContactInfo",
  "description": "Retrieves contact information for a specified user.",
  "parameters": {
    "type": "object",
    "properties": {
      "userId": {
        "type": "integer",
        "description": "The unique identifier for the user."
      },
      "includeAddress": {
        "type": "boolean",
        "description": "Whether to include the user's address in the response."
      }
    },
    "required": [
      "userId"
    ]
  }
}

