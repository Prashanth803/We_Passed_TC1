[
  {
    "name": "createTask",
    "description": "Creates a new task.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the task"
        },
        "priority": {
          "type": "integer",
          "description": "Priority of the task (1-5)"
        },
        "startTime": {
          "type": "string",
          "format": "date-time",
          "description": "Start time of the task"
        },
        "endTime": {
          "type": "string",
          "format": "date-time",
          "description": "End time of the task"
        },
        "status": {
          "type": "integer",
          "description": "Status of the task (1 for pending, 2 for completed)"
        }
      },
      "required": [
        "name",
        "priority",
        "startTime",
        "endTime",
        "status"
      ]
    }
  },
  {
    "name": "editTask",
    "description": "Edits an existing task.",
    "parameters": {
      "type": "object",
      "properties": {
        "taskId": {
          "type": "string",
          "description": "ID of the task to edit"
        },
        "name": {
          "type": "string",
          "description": "Name of the task"
        },
        "priority": {
          "type": "integer",
          "description": "Priority of the task (1-5)"
        },
        "startTime": {
          "type": "string",
          "format": "date-time",
          "description": "Start time of the task"
        },
        "endTime": {
          "type": "string",
          "format": "date-time",
          "description": "End time of the task"
        },
        "status": {
          "type": "integer",
          "description": "Status of the task (1 for pending, 2 for completed)"
        }
      },
      "required": [
        "taskId",
        "name",
        "priority",
        "startTime",
        "endTime",
        "status"
      ]
    }
  },
  {
    "name": "getTaskList",
    "description": "Retrieves a list of tasks based on priority and status.",
    "parameters": {
      "type": "object",
      "properties": {
        "priority": {
          "type": "integer",
          "description": "Priority of the task (0 for all, 1-5 for specific priority)"
        },
        "status": {
          "type": "integer",
          "description": "Status of the task (0 for all, 1 for pending, 2 for completed)"
        }
      },
      "required": [
        "priority",
        "status"
      ]
    }
  },
  {
    "name": "getTables",
    "description": "Retrieves aggregated data for tasks grouped by priority."
  },
  {
    "name": "getDetails",
    "description": "Retrieves overall statistics about tasks."
  },
  {
    "name": "deleteTask",
    "description": "Deletes multiple tasks.",
    "parameters": {
      "type": "object",
      "properties": {
        "taskIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Array of task IDs to delete"
        }
      },
      "required": [
        "taskIds"
      ]
    }
  },
  {
    "name": "authUser",
    "description": "Authenticates a user or creates a new user if one doesn't exist.",
    "parameters": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "User's email address"
        },
        "password": {
          "type": "string",
          "description": "User's password"
        }
      },
      "required": [
        "email",
        "password"
      ]
    }
  }
]

